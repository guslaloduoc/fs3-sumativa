server:
  port: 8082

spring:
  application:
    name: ms-laboratorios

  datasource:
    # Configuración Oracle Cloud ATP - fs3
    url: jdbc:oracle:thin:@fs3_tp?TNS_ADMIN=./wallet
    username: ADMIN
    password: Duocuc@.,2025
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      # validate: no modifica el schema, solo valida que coincida con las entidades
      # Flyway se encarga de las migraciones
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true
    # Evita lazy loading fuera de transacción
    open-in-view: false
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0

logging:
  level:
    com.sumativa.ms_laboratorios: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
