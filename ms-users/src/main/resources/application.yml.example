server:
  port: 8081

spring:
  application:
    name: ms-usuarios

  datasource:
    # Configuración Oracle Cloud ATP - fs3
    # IMPORTANTE: Configure las siguientes variables de entorno antes de ejecutar:
    # - DB_TNS_NAME: Nombre del servicio TNS (ej: fs3_tp)
    # - TNS_ADMIN_PATH: Ruta al directorio del wallet Oracle (ej: ./wallet o ruta absoluta)
    # - DB_USERNAME: Usuario de base de datos (ej: ADMIN)
    # - DB_PASSWORD: Contraseña de base de datos (NUNCA versionar en git)
    url: jdbc:oracle:thin:@${DB_TNS_NAME:fs3_tp}?TNS_ADMIN=${TNS_ADMIN_PATH:./wallet}
    username: ${DB_USERNAME:ADMIN}
    password: ${DB_PASSWORD}
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      # validate: no modifica el schema, solo valida que coincida con las entidades
      # Flyway se encarga de las migraciones
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true
    # Evita lazy loading fuera de transacción
    open-in-view: false
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0

logging:
  level:
    com.sumativa.ms_usuarios: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
